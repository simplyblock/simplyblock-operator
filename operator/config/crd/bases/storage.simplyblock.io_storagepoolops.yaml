---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: storagepoolops.storage.simplyblock.io
spec:
  group: storage.simplyblock.io
  names:
    kind: StoragePoolOps
    listKind: StoragePoolOpsList
    plural: storagepoolops
    shortNames:
    - spops
    singular: storagepoolops
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.poolRef
      name: Pool
      type: string
    - jsonPath: .spec.action
      name: Action
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.step.state
      name: Step
      type: string
    - jsonPath: .status.message
      name: Message
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: |-
          StoragePoolOps is a single operation performed against one StoragePool.
          Analogous to a Kubernetes Job: it drives an action to completion and records
          the result, and only one StoragePoolOps may be active per StoragePool at a
          time, which the pool's status.activeOpsRef enforces.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StoragePoolOpsSpec is one operation to perform against one
              StoragePool.
            properties:
              abort:
                description: Abort asks a running operation to stop at its next step
                  and unwind.
                type: boolean
              action:
                description: |-
                  Action is the operation to perform. Immutable: an operation that changed
                  what it was doing halfway through would have a status describing neither.
                enum:
                - Rebalance
                type: string
                x-kubernetes-validations:
                - message: field is immutable
                  rule: self == oldSelf
              poolRef:
                description: |-
                  PoolRef names the StoragePool this operation acts on, in this object's own
                  namespace. The operation never owns its target, because deleting the
                  record of an operation must not delete the pool it operated on.
                type: string
                x-kubernetes-validations:
                - message: field is immutable
                  rule: self == oldSelf
            required:
            - action
            - poolRef
            type: object
          status:
            description: StoragePoolOpsStatus is the observed state of one pool operation.
            properties:
              completedAt:
                description: CompletedAt is when it reached a terminal phase.
                format: date-time
                type: string
              message:
                description: |-
                  Message is the reason the phase is what it is: one sentence, replaced as
                  the operation moves, and never a log.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the generation the rest of this status was computed
                  from, so a stale status can be told from a current one.
                format: int64
                type: integer
              phase:
                description: Phase is the operation's own progress.
                enum:
                - Pending
                - Running
                - Succeeded
                - Failed
                - Aborted
                type: string
              startedAt:
                description: StartedAt is when the operation acquired its target's
                  lock.
                format: date-time
                type: string
              step:
                description: |-
                  Step is the position of the running action's state machine. It is
                  persisted before the side effect that step performs. The rule repeats the
                  StoragePoolOpsStep enum because a marker cannot reach a field of the
                  shared snapshot type.
                properties:
                  deadline:
                    description: |-
                      Deadline is when that state expires, absent when it has none. It is an
                      absolute instant, so a state whose deadline passed while the controller
                      was down restores as already expired.
                    format: date-time
                    type: string
                  state:
                    description: |-
                      State is the state the machine was in. Empty means the resource has not
                      been reconciled yet, and restores to the graph's initial state.
                    type: string
                type: object
                x-kubernetes-validations:
                - message: unknown step
                  rule: '!has(self.state) || self.state in [''Validating'',''Migrating'']'
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
