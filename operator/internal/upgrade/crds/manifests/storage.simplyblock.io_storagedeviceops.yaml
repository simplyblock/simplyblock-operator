---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: storagedeviceops.storage.simplyblock.io
spec:
  group: storage.simplyblock.io
  names:
    kind: StorageDeviceOps
    listKind: StorageDeviceOpsList
    plural: storagedeviceops
    shortNames:
    - sdops
    singular: storagedeviceops
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.deviceRef
      name: Device
      type: string
    - jsonPath: .spec.action
      name: Action
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.step.state
      name: Step
      type: string
    - jsonPath: .status.message
      name: Message
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha2
    schema:
      openAPIV3Schema:
        description: StorageDeviceOps is a single operation performed against one
          StorageDevice.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StorageDeviceOpsSpec is one operation to perform against
              one StorageDevice.
            properties:
              abort:
                description: |-
                  Abort asks a running operation to stop at its next step and unwind.

                  Restart can be aborted before its call is issued and not after: a restart
                  the control plane has accepted is one nothing can recall, so the graph
                  declares where the edge exists rather than this field promising one.
                type: boolean
              action:
                description: Action is the operation to perform.
                enum:
                - Restart
                type: string
                x-kubernetes-validations:
                - message: field is immutable
                  rule: self == oldSelf
              deviceRef:
                description: |-
                  DeviceRef names the StorageDevice this operation acts on, in this
                  operation's own namespace. The operation never owns its target, because
                  deleting the record of an operation must not delete the device record it
                  operated on.
                maxLength: 253
                type: string
                x-kubernetes-validations:
                - message: field is immutable
                  rule: self == oldSelf
            required:
            - action
            - deviceRef
            type: object
          status:
            description: StorageDeviceOpsStatus is the observed state of one device
              operation.
            properties:
              completedAt:
                description: CompletedAt is when it reached a terminal phase.
                format: date-time
                type: string
              deviceStatusBefore:
                description: |-
                  DeviceStatusBefore is what the control plane reported the device's status
                  to be when the operation took its lock, so a wait can tell the device
                  coming back from its never having gone.
                type: string
              message:
                description: |-
                  Message is the reason the phase is what it is: one sentence, replaced as
                  the operation moves, and never a log.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the generation the rest of this status was computed
                  from, so a stale status can be told from a current one.
                format: int64
                type: integer
              phase:
                description: Phase is the operation's own progress.
                enum:
                - Pending
                - Running
                - Succeeded
                - Failed
                - Aborted
                type: string
              startedAt:
                description: StartedAt is when the operation acquired its target's
                  lock.
                format: date-time
                type: string
              step:
                description: |-
                  Step is the position of the running action's state machine. It is
                  persisted before the side effect that step performs.
                properties:
                  deadline:
                    description: |-
                      Deadline is when that state expires, absent when it has none. It is an
                      absolute instant, so a state whose deadline passed while the controller
                      was down restores as already expired.
                    format: date-time
                    type: string
                  state:
                    description: |-
                      State is the state the machine was in. Empty means the resource has not
                      been reconciled yet, and restores to the graph's initial state.
                    type: string
                type: object
                x-kubernetes-validations:
                - message: unknown step
                  rule: '!has(self.state) || self.state in [''Requesting'',''Awaiting'']'
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
