# SIZE-20

**Mutation.** A 1 GiB pool and a 2 MiB pool, both with free pages on the chosen node

**Expected.** The two sizes are summed into one figure, and nothing says which size the node should take

**Harness.** `CM`
