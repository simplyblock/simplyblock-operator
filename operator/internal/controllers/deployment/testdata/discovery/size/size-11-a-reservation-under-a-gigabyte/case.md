# SIZE-11

**Mutation.** 256 × 2 MiB pages free on the chosen node, 512 MiB in all

**Expected.** Unset, the note saying the smallest reservation is under a gigabyte. A 1.5 GiB reading truncates to `1G`

**Harness.** `CM`
