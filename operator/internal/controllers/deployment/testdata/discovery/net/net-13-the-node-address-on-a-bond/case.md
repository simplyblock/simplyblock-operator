# NET-13

**Mutation.** `bond0` holding the node address over two unaddressed physical slaves

**Expected.** `bond0` named, with `eth0` and `eth1` reported as the hardware under it

**Harness.** `CM`
