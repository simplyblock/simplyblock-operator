# NET-17

**Mutation.** A `macvlan` and an `ipvlan` over `eth0`, all three addressed

**Expected.** `eth0` named: the three carry the same traffic and the physical one is the simplest

**Harness.** `CM`
