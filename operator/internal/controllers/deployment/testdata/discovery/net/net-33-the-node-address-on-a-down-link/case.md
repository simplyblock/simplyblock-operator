# NET-33

**Mutation.** The node's `InternalIP` on a link whose state is `down`

**Expected.** Nothing named: the state rung is applied before the address wins, so a link carrying nothing is refused however right its address is

**Harness.** `CM`
