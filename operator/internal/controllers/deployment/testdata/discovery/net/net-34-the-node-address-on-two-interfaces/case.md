# NET-34

**Mutation.** The node's `InternalIP` on both `bond0` and `bond0.100`

**Expected.** The first by the report's own order, which is the kernel's and is ascending by name, so two runs agree

**Harness.** `CM`
