# NET-15

**Mutation.** `vxlan.calico` holding an overlay address beside `eth0` holding the node address

**Expected.** `eth0` named. The overlay is refused for holding an address the cluster does not reach the machine on

**Harness.** `CM`
