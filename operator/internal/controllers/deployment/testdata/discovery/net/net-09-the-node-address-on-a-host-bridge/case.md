# NET-09

**Mutation.** A worker whose `InternalIP` sits on a bridge, as a hypervisor host's does

**Expected.** `br0` named. A bridge carrying the cluster's own address is the management network

**Harness.** `CM`
