# DEV-17

**Mutation.** An iSCSI LUN beside a virtio disk, block run, no allow list

**Expected.** Only the virtio disk. A LUN is storage across a network and is never taken by default

**Harness.** `CM`
