apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: simplyblock-rebalancer
  namespace: system
  labels:
    app.kubernetes.io/name: simplyblock-operator
    app.kubernetes.io/managed-by: kustomize
spec:
  selector:
    matchLabels:
      role: simplyblock-storage-node
  podMetricsEndpoints:
  - port: rebalancer-metrics
    path: /metrics
    scheme: http