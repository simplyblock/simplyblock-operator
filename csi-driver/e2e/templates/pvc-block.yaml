apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: spdkcsi-pvc-block
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Block
  resources:
    requests:
      storage: 1Gi
  storageClassName: simplyblock-csi-sc
