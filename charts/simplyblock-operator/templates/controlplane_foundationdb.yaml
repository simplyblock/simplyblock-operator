{{- if .Values.operator.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: simplyblock-fdb-controller-manager
  labels:
    control-plane: simplyblock-fdb-controller-manager
    app: simplyblock-fdb-controller-manager
spec:
  selector:
    matchLabels:
      app: simplyblock-fdb-controller-manager
  replicas: 1
  template:
    metadata:
      labels:
        control-plane: simplyblock-fdb-controller-manager
        app: simplyblock-fdb-controller-manager
    spec:
      securityContext:
        runAsUser: 4059
        runAsGroup: 4059
        fsGroup: 4059
      volumes:
        - name: tmp
          emptyDir: {}
        - name: logs
          emptyDir: {}
        - name: fdb-binaries
          emptyDir: {}
      serviceAccountName: simplyblock-fdb-controller-manager
      initContainers:
        - name: foundationdb-kubernetes-init-7-3
          image: {{ .Values.controlplane.foundationdb.image.monitor.repository }}:{{ .Values.controlplane.foundationdb.image.monitor.tag }}
          args:
            - "--copy-library"
            - "7.3"
            - "--copy-binary"
            - "fdbcli"
            - "--copy-binary"
            - "fdbbackup"
            - "--copy-binary"
            - "fdbrestore"
            - "--output-dir"
            - "/var/output-files"
            - "--mode"
            - "init"
          volumeMounts:
            - name: fdb-binaries
              mountPath: /var/output-files
      containers:
        - command:
            - /manager
          args:
            - "--health-probe-bind-address=:9443"
          image: {{ .Values.controlplane.foundationdb.image.operator.repository }}:{{ .Values.controlplane.foundationdb.image.operator.tag }}
          name: manager
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
          ports:
            - name: metrics
              containerPort: 8080
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 500m
              memory: 256Mi
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            privileged: false
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: logs
              mountPath: /var/log/fdb
            - name: fdb-binaries
              mountPath: /usr/bin/fdb
      terminationGracePeriodSeconds: 10

################# ROLE AND ROLE BINDING ##############################
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: simplyblock-fdb-controller-manager

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: simplyblock-fdb-manager-role
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  - events
  - persistentvolumeclaims
  - pods
  - secrets
  - services
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - apps
  resources:
  - deployments
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - apps.foundationdb.org
  resources:
  - foundationdbbackups
  - foundationdbclusters
  - foundationdbrestores
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - apps.foundationdb.org
  resources:
  - foundationdbbackups/status
  - foundationdbclusters/status
  - foundationdbrestores/status
  verbs:
  - get
  - patch
  - update
- apiGroups:
  - coordination.k8s.io
  resources:
  - leases
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  creationTimestamp: null
  name: simplyblock-fdb-manager-clusterrole
rules:
- apiGroups:
  - ""
  resources:
  - nodes
  verbs:
  - get
  - list
  - watch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  creationTimestamp: null
  name: simplyblock-fdb-manager-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: simplyblock-fdb-manager-role
subjects:
- kind: ServiceAccount
  name: simplyblock-fdb-controller-manager
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  creationTimestamp: null
  name: simplyblock-fdb-manager-clusterrolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: simplyblock-fdb-manager-clusterrole
subjects:
- kind: ServiceAccount
  name: simplyblock-fdb-controller-manager
  namespace: metadata.namespace

##### cluster file #################
---
apiVersion: apps.foundationdb.org/v1beta2
kind: FoundationDBCluster
metadata:
  name: simplyblock-fdb-cluster
spec:
  automationOptions:
    replacements:
      enabled: true
  faultDomain:
  {{- if .Values.controlplane.foundationdb.multiAZ }}
    key: topology.kubernetes.io/zone
  {{- else }}
    key: kubernetes.io/hostname
  {{- end }}
  imageType: split
  labels:
    filterOnOwnerReference: false
    matchLabels:
      foundationdb.org/fdb-cluster-name: simplyblock-fdb-cluster
    processClassLabels:
    - foundationdb.org/fdb-process-class
    processGroupIDLabels:
    - foundationdb.org/fdb-process-group-id
  databaseConfiguration:
    redundancy_mode: {{ .Values.controlplane.foundationdb.redundancyMode | default "double" }}
  minimumUptimeSecondsForBounce: 60
  processCounts:
  {{- if .Values.controlplane.foundationdb.multiAZ }}
    cluster_controller: 1
    log: 4
    storage: 4
    stateless: -1
  {{- else }}
    cluster_controller: 1
    log: 3
    storage: 3
    stateless: -1
  {{- end }}
  processes:
    general:
      customParameters:
      - knob_disable_posix_kernel_aio=1
      podTemplate:
        spec:
          containers:
          - name: foundationdb
            resources:
              limits:
                cpu: 300m
                memory: 512Mi
              requests:
                cpu: 100m
                memory: 256Mi
            securityContext:
              runAsUser: 0
          - name: foundationdb-kubernetes-sidecar
            resources:
              limits:
                cpu: 200m
                memory: 256Mi
              requests:
                cpu: 100m
                memory: 128Mi
            securityContext:
              runAsUser: 0
          initContainers:
          - name: foundationdb-kubernetes-init
            resources:
              limits:
                cpu: 100m
                memory: 128Mi
              requests:
                cpu: 100m
                memory: 128Mi
            securityContext:
              runAsUser: 0
      volumeClaimTemplate:
        spec:
          {{- if .Values.controlplane.storageclass.name }}
          storageClassName: {{ .Values.controlplane.storageclass.name }}
          {{- end }}
          accessModes:
            - ReadWriteOnce
          resources:
            requests:
              storage: 10G

    storage:
      podTemplate:
        spec:
          containers:
          - name: foundationdb
            resources:
              limits:
                cpu: 500m
                memory: 4Gi
              requests:
                cpu: 100m
                memory: 1Gi
            securityContext:
              runAsUser: 0
          affinity:
            podAntiAffinity:
              preferredDuringSchedulingIgnoredDuringExecution:
                - weight: 100
                  podAffinityTerm:
                    labelSelector:
                      matchLabels:
                        foundationdb.org/fdb-process-class: storage
                    topologyKey: kubernetes.io/hostname
    log:
      podTemplate:
        spec:
          containers:
          - name: foundationdb
            resources:
              limits:
                cpu: 500m
                memory: 4Gi
              requests:
                cpu: 100m
                memory: 1Gi
            securityContext:
              runAsUser: 0
          affinity:
            podAntiAffinity:
              preferredDuringSchedulingIgnoredDuringExecution:
                - weight: 100
                  podAffinityTerm:
                    labelSelector:
                      matchLabels:
                        foundationdb.org/fdb-process-class: log
                    topologyKey: kubernetes.io/hostname

  routing:
    defineDNSLocalityFields: true
  {{- if .Values.controlplane.foundationdb.image.mainContainer.baseImage }}
  mainContainer:
    imageConfigs:
    - baseImage: {{ .Values.controlplane.foundationdb.image.mainContainer.baseImage }}
  {{- end }}
  sidecarContainer:
    enableLivenessProbe: true
    enableReadinessProbe: false
    {{- if .Values.controlplane.foundationdb.image.sidecarContainer.baseImage }}
    imageConfigs:
    - baseImage: {{ .Values.controlplane.foundationdb.image.sidecarContainer.baseImage }}
    {{- end }}
  useExplicitListenAddress: true
  version: 7.3.63
{{- end }}
