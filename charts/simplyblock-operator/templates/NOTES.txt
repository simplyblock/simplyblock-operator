The Simplyblock Operator is getting deployed to your cluster.

The following components are being installed or updated:
- Simplyblock CSI Driver
{{- if .Values.operator.enabled }}
- Simplyblock Control Plane
{{- end }}

{{- if .Values.operator.enabled }}

The Operator components and its CRDs are installed. Please refer to our documentation to get started:

  https://docs.simplyblock.io/dev/deployments/kubernetes/k8s-storage-plane/
{{- else }}

To follow the pods status, please run:

  kubectl --namespace={{ .Release.Namespace }} get pods --watch
{{- end }}
