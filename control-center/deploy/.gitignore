# The repo-root .gitignore ignores build/ (build output). This build/ holds the
# checked-in build scripts (prepare.mjs, bundle.mjs), not output — re-include
# them, or the image build fails on a missing module.
!build/
!build/*
