---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: storageclassdeployments.fleet.simplyblock.io
spec:
  group: fleet.simplyblock.io
  names:
    kind: StorageClassDeployment
    listKind: StorageClassDeploymentList
    plural: storageclassdeployments
    shortNames:
    - scd
    singular: storageclassdeployment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.memberRef
      name: Member
      type: string
    - jsonPath: .spec.className
      name: Class
      type: string
    - jsonPath: .spec.pool.pool
      name: Pool
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          StorageClassDeployment is one StorageClass drawing on one pool in one member.
          A pool may have zero or more, because nothing about a pool implies a single
          way to consume it.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              StorageClassDeploymentSpec is the class to write in the member, and the pool
              it draws on.
            properties:
              className:
                description: |-
                  ClassName is the StorageClass name in the member. It is immutable because
                  it is the object's name there, and a rename is a different class.
                maxLength: 253
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                type: string
                x-kubernetes-validations:
                - message: field is immutable
                  rule: self == oldSelf
              memberRef:
                description: MemberRef names the FleetMember the class is written
                  in.
                type: string
                x-kubernetes-validations:
                - message: field is immutable
                  rule: self == oldSelf
              pool:
                description: |-
                  Pool is the pool this class draws on. The three fields become the three
                  assignment labels on the class, so an author states the pool and never
                  writes a label by hand.
                properties:
                  cluster:
                    description: Cluster is the StorageCluster the pool belongs to.
                    type: string
                  namespace:
                    description: Namespace is the pool's namespace in the member.
                    type: string
                  pool:
                    description: Pool is the StoragePool's name.
                    type: string
                required:
                - cluster
                - namespace
                - pool
                type: object
                x-kubernetes-validations:
                - message: field is immutable
                  rule: self == oldSelf
              template:
                description: Template is what the class carries beyond its assignment.
                properties:
                  disableVolumeExpansion:
                    description: |-
                      DisableVolumeExpansion turns off online expansion. The negative spelling
                      is what makes the zero value the default, and every class this product
                      writes today allows expansion.
                    type: boolean
                  mountOptions:
                    description: MountOptions are passed to the mount of a volume
                      of this class.
                    items:
                      type: string
                    type: array
                  parameters:
                    additionalProperties:
                      type: string
                    description: |-
                      Parameters are the driver parameters, written in the superseding QoS
                      spelling only, which is what the operator's own generated class does. The
                      three assignment labels are not parameters and are not written here.
                    type: object
                  reclaimPolicy:
                    description: ReclaimPolicy is what happens to a volume when its
                      claim goes.
                    enum:
                    - Delete
                    - Retain
                    type: string
                  volumeBindingMode:
                    description: VolumeBindingMode is when a volume is provisioned.
                    enum:
                    - Immediate
                    - WaitForFirstConsumer
                    type: string
                type: object
            required:
            - className
            - memberRef
            - pool
            - template
            type: object
          status:
            description: StorageClassDeploymentStatus is whether the class reached
              the member.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              delivery:
                description: Delivery is what the ManifestWork carrying this class
                  reports.
                properties:
                  appliedFingerprint:
                    description: AppliedFingerprint is the fingerprint of what actually
                      reached the member.
                    type: string
                  conditions:
                    description: |-
                      Conditions mirror the ManifestWork's Applied, Available, and Degraded,
                      each written against this object's own generation, which the hub issued
                      and can therefore compare.
                    items:
                      description: Condition contains details for one aspect of the
                        current state of this API Resource.
                      properties:
                        lastTransitionTime:
                          description: |-
                            lastTransitionTime is the last time the condition transitioned from one status to another.
                            This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                          format: date-time
                          type: string
                        message:
                          description: |-
                            message is a human readable message indicating details about the transition.
                            This may be an empty string.
                          maxLength: 32768
                          type: string
                        observedGeneration:
                          description: |-
                            observedGeneration represents the .metadata.generation that the condition was set based upon.
                            For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                            with respect to the current state of the instance.
                          format: int64
                          minimum: 0
                          type: integer
                        reason:
                          description: |-
                            reason contains a programmatic identifier indicating the reason for the condition's last transition.
                            Producers of specific condition types may define expected values and meanings for this field,
                            and whether the values are considered a guaranteed API.
                            The value should be a CamelCase string.
                            This field may not be empty.
                          maxLength: 1024
                          minLength: 1
                          pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                          type: string
                        status:
                          description: status of the condition, one of True, False,
                            Unknown.
                          enum:
                          - "True"
                          - "False"
                          - Unknown
                          type: string
                        type:
                          description: type of condition in CamelCase or in foo.example.com/CamelCase.
                          maxLength: 316
                          pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                          type: string
                      required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - type
                    x-kubernetes-list-type: map
                  manifestWorkName:
                    description: ManifestWorkName is the object in the member's namespace
                      on the hub.
                    type: string
                type: object
              message:
                description: Message is why the phase is what it is.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation this status was
                  computed from.
                format: int64
                type: integer
              phase:
                description: Phase is where the class is.
                enum:
                - Delivering
                - Ready
                - PoolMissing
                - Degraded
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
