---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: storagefleets.fleet.simplyblock.io
spec:
  group: fleet.simplyblock.io
  names:
    kind: StorageFleet
    listKind: StorageFleetList
    plural: storagefleets
    shortNames:
    - sf
    singular: storagefleet
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.endpoint
      name: Endpoint
      type: string
    - jsonPath: .status.version
      name: Version
      type: string
    - jsonPath: .status.memberCount
      name: Members
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          StorageFleet is the control plane a fleet is built on, and the fleet's
          defaults. One object per hub, named simplyblock.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              StorageFleetSpec is where the fleet's control plane is, and what its
              deployments inherit.
            properties:
              controlPlane:
                description: |-
                  ControlPlane is the control plane every member of this fleet is joined to.
                  It is immutable as a block, because re-pointing a live fleet at another
                  control plane produces a different fleet.
                properties:
                  credentialsSecretRef:
                    description: CredentialsSecretRef names a Secret in this namespace
                      holding the token.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  endpoint:
                    description: Endpoint is the management API's base URL.
                    pattern: ^https?://[a-zA-Z0-9.-]+(:[0-9]{1,5})?(/.*)?$
                    type: string
                required:
                - credentialsSecretRef
                - endpoint
                type: object
                x-kubernetes-validations:
                - message: field is immutable
                  rule: self == oldSelf
              defaults:
                description: Defaults are the values a member inherits when it omits
                  them.
                properties:
                  inventoryRefreshInterval:
                    description: InventoryRefreshInterval is how often a member is
                      asked to re-probe.
                    type: string
                  opsRetention:
                    description: OpsRetention is how long a terminal FleetOperation
                      is kept.
                    type: string
                type: object
            required:
            - controlPlane
            type: object
          status:
            description: StorageFleetStatus is what the control plane reports about
              itself.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              endpoint:
                description: Endpoint is the resolved base URL, so that a reader asks
                  status not spec.
                type: string
              lastChecked:
                description: LastChecked is when the readiness probe last ran.
                format: date-time
                type: string
              memberCount:
                description: MemberCount is how many FleetMember objects this fleet
                  has.
                format: int32
                type: integer
              message:
                description: Message is why the phase is what it is.
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation this status was
                  computed from.
                format: int64
                type: integer
              phase:
                description: Phase is whether the control plane answers.
                enum:
                - Available
                - Degraded
                - Unavailable
                type: string
              version:
                description: Version is the management API's reported version.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
